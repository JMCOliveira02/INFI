DB Relational Model

client(id, name)
client_order(id, #client_id->client, number, quantity, duedate, latepen, earlypen, piece)
expedition_order(id, #client_order_id->client_order, piece, quantity, expedition_date)
expedition_status(id, #expediton_order_id->expedition_order, end_date)
production_order(id, #client_order_id->client_order, piece, quantity, start_date)
production_status(id, #production_order_id->production_order, end_date)
production_time(id, day, piece, time)
stock(id, day, piece, quantity)
supplier(id, #supply_order_id->supply_order, name, piece, min_order, price_per_piece, delivery_time)
supply_order(id, #client_order_id->client, piece, quantity, buy_date) 
